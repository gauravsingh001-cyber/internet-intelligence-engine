import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Internet Intelligence Engine - Real-Time Company Data & Market Intelligence",
  description: "Access instant insights on 50M+ companies worldwide. Real-time data, advanced analytics, and competitive intelligence powered by AI.",
  keywords: "company data, market intelligence, competitor analysis, business intelligence, B2B data",
  openGraph: {
    title: "Internet Intelligence Engine",
    description: "Real-time company data and market intelligence powered by AI",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
